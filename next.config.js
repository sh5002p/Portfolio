/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',  // Cela indique que tu veux exporter un site statique
    distDir: 'out',    // Cela définit le dossier de sortie pour l'export
  };
  
  module.exports = nextConfig;
  